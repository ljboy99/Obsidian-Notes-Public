FTP is a protocol which is used to transfer files over a network.
It opens a connection to allow commands (port 21) and replies, and another to transfer data (port 20). 
FTP allows for smooth transfers of large files. You can also send multiple files at once. 

FTP Plain is unencrypted
FTPS uses SSL 
FTPES uses TLS / SSL