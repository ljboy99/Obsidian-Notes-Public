Ways to find misconfigurations:

```
ipconfig /all
```
This will show you the configurations of a device (usually a pc)

```
sh interface g0/0.13
```
This will show the statuses of an interface or sub interface. This time this is looking at the sub interface 13 of port 0/0. 